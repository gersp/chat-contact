CREATE TABLE embeddings
(
    hash             TEXT                     NOT NULL PRIMARY KEY,
    text             TEXT                     NOT NULL,
    embedding        TEXT                     NOT NULL
);

